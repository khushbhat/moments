
  # High-Fidelity Web App UI

  This is a code bundle for High-Fidelity Web App UI. The original project is available at https://www.figma.com/design/WAlY6AMoFQZvkBcC8iOvY8/High-Fidelity-Web-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  